{
  "name": "server",
  "version": "1.0.0",
  "description": "A simple JSON server",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "serve": "json-server --watch db.json"
  }
}
